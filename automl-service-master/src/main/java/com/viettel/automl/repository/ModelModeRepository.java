package com.viettel.automl.repository;

import com.viettel.automl.model.ModelModeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelModeRepository extends JpaRepository<ModelModeEntity, Long> {
}
